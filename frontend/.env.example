API_BASE_URL=http://localhost:8000

# Local `astro dev` reads this file.
# For `wrangler dev`, copy `.dev.vars.example` to `.dev.vars`.
# Prefer http://127.0.0.1:8000 in `.dev.vars` so the Worker can reach the host API.
